[workspace]
resolver = "2"
members = ["src-tauri", "src-tauri-cli"]

[profile.release]
panic = "abort"
codegen-units = 1
lto = true
incremental = false
opt-level = "s"
strip = true

[profile.dev]
incremental = true
